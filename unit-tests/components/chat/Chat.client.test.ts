// Chat.client component integration tests (JSX) are in Chat.client.test.tsx.
// This file exists as a non-JSX TypeScript entry for test-mapper compatibility.

import { describe, it, expect } from 'vitest';

describe('app/components/chat/Chat.client.tsx', () => {
  it('has JSX integration tests in Chat.client.test.tsx', () => {
    expect(true).toBe(true);
  });
});
